# Frontend environment variables
# In development, Vite proxy on /api is used so this can be left empty.
# For production builds, set to your backend origin, e.g. https://api.example.com
VITE_API_URL=
# VITE_API_URL can be empty in development since Vite proxy is configured
